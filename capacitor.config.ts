import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.syntropydelta.agroai',
  appName: 'AgroAI Suite Pro',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
